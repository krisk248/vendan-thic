# Adhyatma Vidya - Environment Configuration Template
# Copy this file to .env and set your Google Sheets URLs

# Google Sheets CSV Export URLs
# To get these URLs:
# 1. Open your Google Sheet
# 2. Go to File > Share > Publish to web
# 3. Choose "Comma-separated values (.csv)" and select the specific sheet
# 4. Copy the generated URL and paste below

# Flashcards Google Sheets CSV URL
# Format: https://docs.google.com/spreadsheets/d/{SHEET_ID}/export?format=csv&gid={GID}
FLASHCARDS_SHEET_URL=

# Quiz Questions Google Sheets CSV URL  
# Format: https://docs.google.com/spreadsheets/d/{SHEET_ID}/export?format=csv&gid={GID}
QUIZ_SHEET_URL=

# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration (optional)
# DB_PATH=./server/database.db

# Email Configuration (for future contact form)
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=

# Admin Configuration (for future admin panel)
# ADMIN_EMAIL=
# ADMIN_PASSWORD=